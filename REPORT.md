# Практична робота — Аналіз та оптимізація коду

## 1. Мета
Я хотів подивитися, де код гальмує і чому це взагалі відбувається. Ще я зробив версію після виправлень, щоб порівняти час, пам'ять і стиль коду.

## 2. Які інструменти я вибрав
- pylint — я взяв його, бо він швидко показує помилки стилю, зайві змінні і довгі рядки.
- cProfile — я використав його, щоб побачити, яка частина коду забирає найбільше часу.
- timeit — я взяв цей модуль, бо ним зручно порівнювати швидкість двох варіантів коду.
- tracemalloc — я перевірив пам'ять через нього, щоб зрозуміти, чи є зайві витрати.

## 3. Аналіз поганого коду
Я запустив `pylint` на `bad_code.py` і ще окремо прогнав скрипт профілювання. Загалом я знайшов невикористану змінну, дублювання коду, лінійний пошук по списку, довгі рядки, порушення PEP8 і магічні числа без констант.

Я ще побачив, що в одному місці код двічі будує майже однаковий список, а в іншому шукає користувача через перебір всього списку. Ну і це все робить код повільнішим, ніж треба.

```text
bad_code.py:1:0: C0114 Missing module docstring
bad_code.py:8:8: W0612 Unused variable 'x'
bad_code.py:16:12: C0301 Line too long (118/79 characters)
bad_code.py:38:12: C0200 Consider using enumerate instead of iterating with range and len
bad_code.py:49:8: C0301 Line too long (273/79 characters)
bad_code.py:76:0: W0511 Consider using a dictionary instead of linear search here
```

## 4. Що я виправив
| Проблема | Що зробив | Результат |
|---|---|---|
| Невикористані змінні | Прибрав зайві змінні або почав їх використовувати | Код став чистіший |
| Дублювання | Замінив подвійне складання списку на одну побудову через list comprehension | Менше коду і менше зайвих дій |
| Довгі рядки | Розбив довгі f-string на декілька рядків | Стало по PEP8 |
| Magic numbers | Виніс числа в константи | Легше читати і змінювати |
| Лінійний пошук | Замінив перебір списку на словник | Пошук став швидший |
| Зайві цикли | Там, де можна, використав list comprehension і `sum()` | Код став коротший і швидший |

## 5. Порівняння швидкості
Я запустив `timeit` і порівняв два варіанти на однакових даних. У моєму запуску було так: `bad_code.py` — 0.0271с, а `good_code.py` — 0.0107с. Загалом стало швидше десь у 2.54 раза.

## 6. Використання пам'яті
Через `tracemalloc` я подивився пікову пам'ять для обох версій. У поганого коду пік був 1.1 KiB, а у хорошого — 0.5 KiB. Різниця невелика, але все ж хороша версія витрачала менше.

## 7. Профілювання через cProfile
`cProfile` показав, що найбільше часу йшло на обхід списку користувачів у `process_tasks`, а ще трохи на побудову звіту в `build_task_report`. Саме там був вузький момент, бо пошук робився в циклі для кожної задачі. Після переходу на словник це місце стало спокійніше, і у хорошій версії сумарний час був менший.

## 8. Тестування
Я вручну запустив обидва файли і перевірив, що вони повертають однакові базові результати. Ще я окремо прогнав `profiling_test.py`, щоб побачити час, пам'ять і профіль. Після виправлень код уже не виглядав перевантаженим, і читати його стало простіше. Ну і в консолі все відпрацювало без помилок.

## 9. Висновок
Я побачив, що навіть прості дрібниці типу зайвого циклу або лінійного пошуку можуть трохи гальмувати програму. Ще я переконався, що pylint добре знаходить стилістичні проблеми, а профілювання допомагає знайти справжнє вузьке місце. Загалом після виправлень код став чистіший, коротший і трохи швидший.# Звіт до практичної роботи

**Тема:** Розробка веб-додатку з використанням фреймворку Flask

## 1. Мета роботи

Метою цієї практичної роботи було створення веб-додатку для обліку задач з використанням фреймворку Flask. У процесі виконання було опрацьовано роботу з шаблонами, формами, базою даних та авторизацією користувача.

Окремо було вивчено принцип збереження даних у SQLite та застосування базового захисту сторінок. У результаті отримано простий і працездатний веб-додаток.

## 2. Обґрунтування вибору Flask

Для виконання роботи було обрано Flask, оскільки цей фреймворк є простим для вивчення та зручним для створення невеликих веб-проєктів. Він не перевантажений складними механізмами і дозволяє зосередитися на основних принципах побудови сайту.

Також Flask добре підходить для поєднання з SQLAlchemy, Flask-Login і Flask-WTF. Це дало змогу зібрати весь потрібний набір інструментів в одному проєкті.

## 3. План виконання роботи

- створити структуру проєкту для веб-додатку
- підключити SQLite як базу даних
- розробити моделі User та Task
- написати форми для входу, реєстрації та додавання задач
- реалізувати маршрути для основних сторінок і дій користувача
- перевірити роботу авторизації, додавання задач і захисту сторінок

## 4. Схема бази даних

У роботі використано дві таблиці: `User` та `Task`.

**Таблиця User:**
- `id` — унікальний ідентифікатор користувача
- `username` — ім'я користувача
- `email` — електронна пошта
- `password` — хеш пароля

**Таблиця Task:**
- `id` — унікальний ідентифікатор задачі
- `title` — назва задачі
- `description` — опис задачі
- `done` — ознака виконання
- `created_at` — дата створення
- `user_id` — зв'язок із користувачем

## 5. Структура проєкту

```text
brud/
├── REPORT.md
├── README.md
├── images/
│   ├── photo1.jpg
│   ├── photo2.jpg
│   └── photo3.jpg
└── todo_app/
        ├── app.py
        ├── config.py
        ├── extensions.py
        ├── forms.py
        ├── models.py
        ├── routes.py
        ├── requirements.txt
        ├── static/
        │   └── style.css
        └── templates/
                ├── base.html
                ├── dashboard.html
                ├── index.html
                ├── login.html
                └── register.html
```

## 6. Опис виконаних робіт по файлах

### app.py

У файлі `app.py` було створено Flask-додаток, підключено конфігурацію та базу даних. Також було зареєстровано blueprint і виконано створення таблиць через `db.create_all()`.

### models.py

У файлі `models.py` були описані моделі `User` і `Task`. Для паролів було використано хешування через `werkzeug.security`, щоб не зберігати їх у відкритому вигляді.

### forms.py

У файлі `forms.py` були створені форми `LoginForm`, `RegisterForm` та `TaskForm`. Для полів додано перевірку введених даних, щоб уникнути пустих значень та помилок у форматі email.

### routes.py

У файлі `routes.py` були реалізовані маршрути для головної сторінки, входу, реєстрації, виходу, робочої панелі та операцій із задачами. Тут виконано додавання, позначення виконання та видалення задач.

## 7. Скріншоти

Скріншоти для звіту мають бути розміщені у папці `images/` з назвами `photo1.jpg`, `photo2.jpg`, `photo3.jpg`.

<p align="center">
    <img src="images/photo1.jpg" alt="Фото 1" width="100%">
</p>

<p align="center">
    <img src="images/photo2.jpg" alt="Фото 2" width="100%">
</p>

<p align="center">
    <img src="images/photo3.jpg" alt="Фото 3" width="100%">
</p>

## 8. Захист проєкту

У проєкті використано CSRF-захист через Flask-WTF, що дозволяє захистити форми від підроблених запитів. У шаблонах для цього використовується `hidden_tag()`.

Також застосовано хешування паролів, тому пароль зберігається у безпечнішому вигляді. Додатково використано `login_required`, щоб доступ до сторінки задач був лише після авторизації.

## 9. Тестування

Під час перевірки було протестовано реєстрацію нового користувача, вхід у систему та вихід з акаунта. Також було перевірено додавання задач, позначення їх як виконаних і видалення.

Окремо було перевірено, що без авторизації доступ до робочої панелі закритий. Після входу сторінка працює коректно.

## 10. Висновок

У ході виконання практичної роботи було створено простий веб-додаток на Flask із використанням бази даних, форм і авторизації. Було закріплено навички роботи з моделями, маршрутами та шаблонами Jinja2.

Також було отримано практичний досвід у підключенні CSRF-захисту, хешуванні паролів і обмеженні доступу до сторінок. Робота допомогла краще зрозуміти структуру невеликого веб-проєкту.
